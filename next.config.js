/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.svgrepo.com", "i0.wp.com", "images.unsplash.com"],
  },
};

module.exports = nextConfig;
